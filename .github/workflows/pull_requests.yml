name: Pull Requests

on:
  pull_request:

env:
  PYTHONUNBUFFERED: "1"
  PYTEST_ADDOPTS: "--color=yes"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test-qemu:
    name: QEMU ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: malta-be
            firmware: openwrt-24.10.2-malta-be-vmlinux-initramfs.elf
            dependency: qemu-system-mips

          - target: x86-64
            firmware: openwrt-24.10.2-x86-64-generic-squashfs-combined.img.gz
            dependency: qemu-system-x86

          - target: armsr-armv8
            firmware: openwrt-24.10.2-armsr-armv8-generic-initramfs-kernel.bin
            dependency: qemu-system-aarch64

    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Setup environment
        run: |
          sudo apt-get update
          sudo apt-get -y install \
            ${{ matrix.dependency }}

          echo "LG_ENV=targets/qemu-${{ matrix.target }}.yaml" >> $GITHUB_ENV

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Download test firmware
        env:
          target: ${{ matrix.target }}
        run: |
          wget https://downloads.openwrt.org/releases/24.10.2/targets/${target/-/\/}/${{ matrix.firmware }} \
            --output-document ${{ matrix.firmware }}

      - name: Run test
        run: |
          gunzip ${{ matrix.firmware }} || true
          firmware=${{ matrix.firmware }}

          uv run pytest tests/ \
            --lg-log \
            --lg-colored-steps \
            --log-cli-level=CONSOLE \
            --firmware $GITHUB_WORKSPACE/${firmware/.gz/}

      - name: Upload console logs
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: console-${{ matrix.target }}
          path: console_*

  test-real:
    name: Device ${{ matrix.device }}
    runs-on: global-coordinator
    strategy:
      fail-fast: false
      matrix:
        include:
          - device: openwrt-one
            target: mediatek-filogic
            firmware: openwrt-24.10.2-mediatek-filogic-openwrt_one-initramfs.itb
            proxy: labgrid-aparcar

          - device: turris-omnia
            target: mvebu-cortexa9
            firmware: openwrt-24.10.2-mvebu-cortexa9-cznic_turris-omnia-initramfs-kernel.bin
            proxy: labgrid-bastian

          - device: tplink_tl-wdr3600-v1
            target: ath79-generic
            firmware: openwrt-24.10.2-ath79-generic-tplink_tl-wdr3600-v1-initramfs-kernel.bin
            proxy: labgrid-aparcar

          - device: genexis-pulse-ex400
            target: ramips-mt7621
            firmware: openwrt-24.10.2-ramips-mt7621-genexis_pulse-ex400-initramfs-kernel.bin
            proxy: labgrid-aparcar

    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Download test firmware
        env:
          target: ${{ matrix.target }}
        run: |
          mkdir -p /tmp/tftp/${{ matrix.device }}
          wget https://downloads.openwrt.org/releases/24.10.2/targets/${target/-/\/}/${{ matrix.firmware }} \
            --output-document /tmp/tftp/${{ matrix.device }}/${{ matrix.firmware }}

      - name: Set environment variables
        run: |
          echo "LG_IMAGE=/tmp/tftp/${{ matrix.device }}/${{ matrix.firmware }}" >> $GITHUB_ENV
          echo "LG_PROXY=${{ matrix.proxy }}" >> $GITHUB_ENV

      - name: Wait for free device
        run: |
          eval $(uv run labgrid-client reserve --wait --shell device=${{ matrix.device }})
          echo "LG_TOKEN=$LG_TOKEN" >> $GITHUB_ENV
          echo "LG_PLACE=+" >> $GITHUB_ENV
          uv run labgrid-client -p +$LG_TOKEN lock
          echo "LG_ENV=targets/${{ matrix.device }}.yaml" >> $GITHUB_ENV

      - name: Run test
        run: |
          uv run pytest tests/ \
            --lg-log \
            --lg-colored-steps \
            --log-cli-level=CONSOLE

      - name: Poweroff and unlock device
        if: always()
        run: |
          uv run labgrid-client power off
          uv run labgrid-client unlock

      - name: Upload console logs
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: console-${{ matrix.target }}
          path: console_*
