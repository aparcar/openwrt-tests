<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="25" failures="0" skipped="8" tests="33" time="0.819" timestamp="2025-10-21T15:27:34.440141+00:00" hostname="openwrt-testing"><properties><property name="ENV_CONFIG" value="targets/linksys_e8450.yaml" /><property name="TARGETS" value="['main']" /></properties><testcase classname="tests.test_apk.TestApk" name="test_apk_procd_installed" time="0.256"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_apk.TestApk" name="test_apk_add_ucert" time="0.000"><skipped type="pytest.skip" message="LG_FEATURE_ONLINE not defined">/home/aparcar/actions-runner/_work/openwrt-tests/openwrt-tests/tests/test_apk.py:13: LG_FEATURE_ONLINE not defined</skipped></testcase><testcase classname="tests.test_apk.TestApk" name="test_apk_audit" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_base" name="test_shell" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_base" name="test_firmware_version" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_base" name="test_dropbear_startup" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_base" name="test_ssh" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_base" name="test_echo" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_base" name="test_uname" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_base" name="test_ubus_system_board" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_base" name="test_free_memory" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_base" name="test_sysupgrade_backup" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'rootfs'}&quot; is not supported">/home/aparcar/actions-runner/_work/openwrt-tests/openwrt-tests/tests/test_base.py:69: Skipping because feature "{'rootfs'}" is not supported</skipped></testcase><testcase classname="tests.test_base" name="test_sysupgrade_backup_u" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'rootfs'}&quot; is not supported">/home/aparcar/actions-runner/_work/openwrt-tests/openwrt-tests/tests/test_base.py:81: Skipping because feature "{'rootfs'}" is not supported</skipped></testcase><testcase classname="tests.test_base" name="test_kernel_errors" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_lan" name="test_lan_wait_for_network" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_lan" name="test_lan_interface_address" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_network_security" name="test_ssh_supported_algorithms" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_opkg" name="test_opkg_procd_installed" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'opkg'}&quot; is not supported">/home/aparcar/actions-runner/_work/openwrt-tests/openwrt-tests/tests/test_opkg.py:4: Skipping because feature "{'opkg'}" is not supported</skipped></testcase><testcase classname="tests.test_opkg" name="test_opkg_install_ucert" time="0.000"><skipped type="pytest.skip" message="Skipping because features &quot;{'online', 'opkg'}&quot; are not supported">/home/aparcar/actions-runner/_work/openwrt-tests/openwrt-tests/tests/test_opkg.py:9: Skipping because features "{'online', 'opkg'}" are not supported</skipped></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_cpu_load" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_memory_usage" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_filesystem_usage" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_system_uptime" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_temperature_sensors" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_process_count" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_entropy_available" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_system_health.TestSystemHealth" name="test_open_file_descriptors" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_wan" name="test_wan_wait_for_network" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_wan" name="test_https_download" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'online'}&quot; is not supported">/home/aparcar/actions-runner/_work/openwrt-tests/openwrt-tests/tests/test_wan.py:56: Skipping because feature "{'online'}" is not supported</skipped></testcase><testcase classname="tests.test_wifi" name="test_wifi_wpa3" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_wifi" name="test_wifi_wpa2" time="0.000"><error message="failed on setup with &quot;labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))&quot;">self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
&gt;                           self.get_resource(requirement, name=supplier_name, wait_avail=False),
                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/target.py:363: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
cls = &lt;class 'labgrid.resource.power.NetworkPowerPort'&gt;

    def get_resource(self, cls, *, name=None, wait_avail=True):
        """
        Helper function to get a resource of the target.
        Returns the first valid resource found, otherwise a
        NoResourceFoundError is raised.
    
        Arguments:
        cls -- resource-class to return as a resource
        name -- optional name to use as a filter
        wait_avail -- wait for the resource to become available (default True)
        """
        found = []
        other_names = []
        default = None
        if isinstance(cls, str):
            cls = target_factory.class_from_string(cls)
    
        for res in self.resources:
            if not isinstance(res, cls):
                continue
            if res.name == "default":
                default = res
            if name and res.name != name:
                other_names.append(res.name)
                continue
            found.append(res)
    
        # if no explicit resource name is requested and a "default" resource was saved and
        # multiple resources were found, use the default resource
        if not name and default and len(found) != 1:
            found = [default]
    
        if not found:
            name_msg = f" named '{name}'" if name else ""
            if other_names:
                raise NoResourceFoundError(
                    f"no {cls.__name__} resource{name_msg} found in {self}, matching resources with other names: {other_names}"  # pylint: disable=line-too-long
                )
    
&gt;           raise NoResourceFoundError(
                f"no {cls.__name__} resource{name_msg} found in {self}"
E               labgrid.exceptions.NoResourceFoundError: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:147: NoResourceFoundError

The above exception was the direct cause of the following exception:

request = &lt;SubRequest 'env' for &lt;Function test_apk_procd_installed&gt;&gt;
record_testsuite_property = &lt;bound method LogXML.add_global_property of &lt;_pytest.junitxml.LogXML object at 0x7429d6f08590&gt;&gt;

    @pytest.fixture(scope="session")
    def env(request, record_testsuite_property):
        """Return the environment configured in the supplied configuration file.
        It contains the targets contained in the configuration file.
        """
        env = request.config.stash[LABGRID_ENV_KEY]
    
        if not env:
            pytest.skip("missing environment config (use --lg-env)")
    
        record_testsuite_property('ENV_CONFIG', env.config_file)
        targets = list(env.config.get_targets().keys())
        record_testsuite_property('TARGETS', targets)
    
        for target_name in targets:
            try:
&gt;               target = env.get_target(target_name)
                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^

.venv/lib/python3.13/site-packages/labgrid/pytestplugin/fixtures.py:70: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
.venv/lib/python3.13/site-packages/labgrid/environment.py:49: in get_target
    target = target_factory.make_target(role, config, env=self)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/factory.py:159: in make_target
    self.make_driver(target, driver, name, args)
.venv/lib/python3.13/site-packages/labgrid/factory.py:138: in make_driver
    d = cls(target, name, **args)
        ^^^^^^^^^^^^^^^^^^^^^^^^^
.venv/lib/python3.13/site-packages/labgrid/driver/powerdriver.py:157: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/driver/common.py:25: in __attrs_post_init__
    super().__attrs_post_init__()
.venv/lib/python3.13/site-packages/labgrid/binding.py:55: in __attrs_post_init__
    target.bind(self)
.venv/lib/python3.13/site-packages/labgrid/target.py:431: in bind
    return self.bind_driver(bindable)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

self = Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))
client = NetworkPowerDriver(target=None, name=None, state=&lt;BindingState.idle: 0&gt;, delay=2.0)

    def bind_driver(self, client):
        """
        Bind the driver to all suppliers (resources and other drivers).
    
        Currently, we only support binding all suppliers at once.
        """
        if client.state is not BindingState.idle:
            raise BindingError(
                f"{client} is not in state {BindingState.idle}"
            )
    
        # consistency check
        assert isinstance(client, Driver)
        assert client not in self.drivers
        assert client.target is None
    
        mapping = self._binding_map
        self._binding_map = {}
    
        # locate suppliers
        bound_suppliers = []
        bound_req_pairs = set()
        for name, requirements in client.bindings.items():
            explicit = False
            if isinstance(requirements, Driver.NamedBinding):
                requirements = requirements.value
                explicit = True
            supplier_name = mapping.pop(name, None)
            if explicit and supplier_name is None:
                raise BindingError(
                    f"supplier for {name} ({requirements}) of {client} in {self} requires an explicit name"  # pylint: disable=line-too-long
                )
            # use sets even for a single requirement and make a local copy
            if not isinstance(requirements, set):
                requirements = {requirements}
            else:
                requirements = requirements.copy()
            # None indicates that the binding is optional
            optional = None in requirements
            requirements.discard(None)
    
            errors = []
            suppliers = []
            for requirement in requirements:
                # convert class name string to classes
                if isinstance(requirement, str):
                    requirement = target_factory.class_from_string(requirement)
                try:
                    if issubclass(requirement, Resource):
                        suppliers.append(
                            self.get_resource(requirement, name=supplier_name, wait_avail=False),
                        )
                    elif issubclass(requirement, (Driver, abc.ABC)): # all Protocols derive from ABC
                        suppliers.append(
                            self.get_driver(requirement, name=supplier_name, activate=False),
                        )
                    else:
                        raise NoSupplierFoundError(f"invalid binding type {requirement}")
                except NoSupplierFoundError as e:
                    errors.append(e)
            if not suppliers:
                client_name = client.name or client.__class__.__name__
                if optional:
                    supplier = None
                elif len(errors) == 1:
                    err = errors[0]
                    err_cls = type(err)
&gt;                   raise err_cls(f"binding {client_name} failed: {err}") from err
E                   labgrid.exceptions.NoResourceFoundError: binding NetworkPowerDriver failed: no NetworkPowerPort resource found in Target(name='main', env=Environment(config_file='targets/linksys_e8450.yaml'))

.venv/lib/python3.13/site-packages/labgrid/target.py:380: NoResourceFoundError</error></testcase><testcase classname="tests.test_wifi" name="test_wifi_scan" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'wifi-neighbors'}&quot; is not supported">/home/aparcar/actions-runner/_work/openwrt-tests/openwrt-tests/tests/test_wifi.py:76: Skipping because feature "{'wifi-neighbors'}" is not supported</skipped></testcase><testcase classname="tests.test_wifi" name="test_wifi_hwsim_sae_mixed" time="0.000"><skipped type="pytest.skip" message="Skipping because feature &quot;{'hwsim'}&quot; is not supported">/home/aparcar/actions-runner/_work/openwrt-tests/openwrt-tests/tests/test_wifi.py:97: Skipping because feature "{'hwsim'}" is not supported</skipped></testcase></testsuite></testsuites>